# Signup API resources

This folder documents the REST contract used by the signup feature.

- `openapi.yaml` defines the resource model and HTTP operations.
- `sign-up.http` contains ready-to-run request examples.

The Angular signup feature posts account creation to `POST /api/users`.
